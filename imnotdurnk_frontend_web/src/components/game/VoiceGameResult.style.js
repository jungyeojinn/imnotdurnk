import { styled } from 'styled-components';

const ResultContainer = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3rem;
`;

const ButtonDiv = styled.div`
    display: flex;
    flex-direction: column;
    gap: 1rem;

    width: 100%;
`;

export { ButtonDiv, ResultContainer };
